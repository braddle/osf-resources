pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo "build stage..."
                sh "pwd"
                sh "ls"
            }
        }

        stage("test") {
            steps {
                echo "build stage..."
            }
        }

        stage("deploy") {
            steps {
                echo "build stage..."
            }
        }
    }
}